# Vault Configuration
VAULT_ADDR=http://vault.vault.svc.cluster.local:8200
VAULT_ROLE_ID=
VAULT_SECRET_ID=
VAULT_PATH=secret/data/tatarlang

# PostgreSQL Secrets (from Vault)
POSTGRES_USER=ref+vault://secret/tatarlang/db#POSTGRES_USER
POSTGRES_PASSWORD=ref+vault://secret/tatarlang/db#POSTGRES_PASSWORD

# RabbitMQ Secrets (from Vault)
RABBITMQ_USER=ref+vault://secret/tatarlang/rabbitmq#RABBITMQ_USER
RABBITMQ_PASS=ref+vault://secret/tatarlang/rabbitmq#RABBITMQ_PASS

# Celery Secrets (from Vault)
CELERY_BROKER_URL=ref+vault://secret/tatarlang/celery#CELERY_BROKER_URL

# Application Configuration
POSTGRES_DB=tatarlang
POSTGRES_HOST=db
POSTGRES_PORT=5432
DJANGO_SETTINGS_MODULE=tatarlang.settings
REACT_APP_API_URL=https://tatarlang.local/api/v1

